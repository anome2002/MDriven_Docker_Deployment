# MDriven Full Deployment (Docker)

## 🚀 Quick Start

1. Build and run the full stack:
   ```bash
   docker compose up -d
   ```

2. Access:
   - Turnkey frontend → http://localhost:8080
   - MDriven Server → http://localhost:5010

3. Upload your .ecomodel via the MDriven Server admin interface.

4. Restart Turnkey to load your model.

---

This setup includes both MDriven Server and Turnkey containers on the same network.
